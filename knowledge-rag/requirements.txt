# Mirror of [project.dependencies] in pyproject.toml.
# Source of truth is pyproject.toml — regenerate this file when deps change.
#
# Install everything (including dev tools) with:
#   pip install -e ".[dev]"
# or, when CI / hosts cannot use editable installs, install plain runtime deps:
#   pip install -r requirements.txt

python-docx>=1.1
mammoth>=1.8
qdrant-client>=1.9
FlagEmbedding>=1.3
langgraph>=0.2
langchain-core>=0.3
anthropic>=0.30
openai>=1.40
streamlit>=1.35
openpyxl>=3.1
ragas>=0.1
datasets>=2.0
python-dotenv>=1.0
tiktoken>=0.7
tqdm>=4.66
PyYAML>=6.0
pypdf>=4.0
pdfplumber>=0.11
beautifulsoup4>=4.12
markdown-it-py>=2.2
